<!-- Loop over each object in the projects array -->
{{#each posts as |post| }}
<div class="row my-4 border">
  <div class="col-md-5 my-2">
    <h2>{{post.title}}</h2>
  </div>
  <div class="col-md-7 my-3">
    <p>
      {{post.content}}
    </p>
    <p>Posted by {{post.user.name}} on {{format_date post.date_created}}</p>
    {{!-- <form class="form new-comment-form">
      <div class="form-group">
        <textarea class="form-input" id="comment-content" name="comment-content"></textarea>
      </div>
      <div class="form-group">
        <button type="submit" class="btn btn-primary">Add Comment</button>
      </div>
    </form> --}}
  </div>
</div>

{{!-- {{#if comments.length}}
<div class="col-md-6">
  <h3>Comments</h3> --}}

  {{!-- {{#each comments as |comment|}}
  <div class="row mb-2">
    <div class="col-md-8">
      <p href="/comments/{{comment.id}}">{{comment.content}}</p>
    </div>
    <div class="col-md-4">
      <button class="btn btn-sm btn-success" data-id="{{comment.id}}">Edit Comment</button>
    </div>
  </div>
  {{/each}}
</div>
{{/if}}
{{/each}}

<script src="./js/comments.js"></script> --}}